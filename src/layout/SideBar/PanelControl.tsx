import SearchBar from '../../components/SearchBar';

const PanelControl = () => {
    return (
        <div className="panel-control">
            <SearchBar />
        </div>
    );
};
export default PanelControl;
